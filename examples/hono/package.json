{
  "name": "hono",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev_vite": "vite",
    "dev": "node --experimental-strip-types server",
    "build": "pnpm build:client && pnpm build:server",
    "build:client": "vite build --outDir dist/client --mode=production",
    "build:server": "vite build --ssr src/entry-server.ts --outDir dist/server --mode=production",
    "preview": "pnpm build && pnpm start",
    "start": "NODE_ENV=production node --experimental-strip-types server"
  },
  "dependencies": {
    "@hono/node-server": "1.13.0",
    "hono": "^4.7.9",
    "vite-uni-rpc": "git+https://github.com/thednp/vite-uni-rpc.git"
  },
  "devDependencies": {
    "@hono/vite-dev-server": "^0.19.0",
    "@types/node": "^22.15.18",
    "cross-env": "^7.0.3",
    "typescript": "~5.7.3",
    "vite": "^6.3.5"
  }
}